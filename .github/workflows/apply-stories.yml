name: Apply Table Redesign
on:
  workflow_dispatch:
  push:
    paths:
      - 'assets/table-redesign.css'
      - 'assets/table-redesign.js'
      - 'assets/table-cinematic.css'
      - '.github/workflows/apply-stories.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install final redesign
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          h=p.read_text(encoding='utf-8')
          h=re.sub(r'<style id="stories-immersive-style">.*?</style>\s*','',h,flags=re.S)
          h=re.sub(r'<script id="stories-immersive-script">.*?</script>\s*','',h,flags=re.S)
          h=re.sub(r'<style id="table-experience-redesign">.*?</style>\s*','',h,flags=re.S)
          h=re.sub(r'<script id="table-experience-redesign-script">.*?</script>\s*','',h,flags=re.S)
          h=re.sub(r'<link rel="stylesheet" href="assets/table-redesign.css">\s*','',h)
          h=re.sub(r'<link rel="stylesheet" href="assets/table-cinematic.css">\s*','',h)
          h=re.sub(r'<script src="assets/table-redesign.js"></script>\s*','',h)
          h=re.sub(r'<h1>Every table tells a <em>[^<]*</em></h1>', '<h1>Every table tells a <em>tale.</em></h1>', h, count=1)
          h=h.replace('</head>','<link rel="stylesheet" href="assets/table-redesign.css">\n<link rel="stylesheet" href="assets/table-cinematic.css">\n</head>',1)
          h=h.replace('</body>','<script src="assets/table-redesign.js"></script>\n</body>',1)
          p.write_text(h,encoding='utf-8')
          PY
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/workflows/apply-stories.yml assets/table-redesign.css assets/table-redesign.js assets/table-cinematic.css
          git diff --cached --quiet || git commit -m "Implement cinematic map to thread experience"
          git push
